// Variable: A discrete location in memory reserved for the storage of data for a specific period of time
let firstName;
{
    // Object: A variable containing a tree of list of key/value pairs. 
    const ENORMOUS_FILE = {name: "Amber", age: "34"};
    firstName = ENORMOUS_FILE.name;
}

console.log(`Hello World's Cutest ${firstName}`);
